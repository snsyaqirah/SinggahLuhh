# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0                # PostgreSQL async driver (0.30+ required for Python 3.13)
alembic==1.13.3
supabase==2.10.0               # Supabase client for auth & realtime
postgis==1.0.4                 # PostGIS helpers

# Pydantic & settings
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Auth
python-jose[cryptography]==3.3.0    # JWT
passlib[bcrypt]==1.7.4              # password hashing
httpx==0.27.2                       # Google OAuth HTTP calls
authlib==1.3.2                      # OAuth client

# Utilities
python-multipart==0.0.12            # form data support
python-slugify==8.0.4               # masjid name → slug
geopy==2.4.1                        # Haversine / geocoding helpers
emails==0.6.0                       # SMTP email sending

# Dev / testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
